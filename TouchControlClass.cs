using UnityEngine;
using System.Collections;

public class TouchControlClass : MonoBehaviour {
	public string Control;				//This is the control so if the button is pressed then put this string in the TouchClass for processing

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
